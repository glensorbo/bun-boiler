# Local OpenPanel reverse proxy — HTTP only, no TLS
# Serves the dashboard on :8091; the API is accessible directly on port 3001.
:8091 {
    encode zstd gzip

    handle /api/* {
        reverse_proxy op-api:3000
    }

    handle {
        reverse_proxy op-dashboard:3000
    }
}
