name: 🎭 E2E Tests

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '0 2 * * 1'  # every Monday at 02:00 UTC

jobs:
  e2e:
    name: 🧪 Docker E2E Suite
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v6

      - name: 🐳 Run self-contained E2E suite
        run: ./scripts/e2e-ci.sh
